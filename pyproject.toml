[project]
name = "daily-writer"
version = "0.1.0"
description = "A simple daily report generator for HCM implementation work."
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]

[tool.vercel]
entrypoint = "api.generate:handler"
